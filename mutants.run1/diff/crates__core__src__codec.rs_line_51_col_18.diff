--- crates/core/src/codec.rs
+++ replace >= with < in read_varint
@@ -43,17 +43,17 @@
     let mut value: u32 = 0;
     let mut shift = 0;
     for (idx, &byte) in bytes.iter().enumerate() {
         value |= u32::from(byte & 0x7F) << shift;
         if byte & 0x80 == 0 {
             return (value, idx + 1);
         }
         shift += 7;
-        if shift >= 35 {
+        if shift < /* ~ changed by cargo-mutants ~ */ 35 {
             break;
         }
     }
     (0, bytes.len())
 }
 
 /// Encodes a strictly increasing sequence as deltas, appending to `out`.
 ///
