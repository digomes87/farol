--- crates/core/src/topk.rs
+++ replace <impl PartialOrd for Candidate>::partial_cmp -> Option<Ordering> with Some(Default::default())
@@ -43,17 +43,17 @@
             // evicted: ties resolve towards the lower id, matching the order a
             // full sort produces.
             .then(self.doc.cmp(&other.doc))
     }
 }
 
 impl PartialOrd for Candidate {
     fn partial_cmp(&self, other: &Self) -> Option<Ordering> {
-        Some(self.cmp(other))
+        Some(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 /// Keeps the best `k` documents seen so far.
 #[derive(Debug)]
 pub struct TopK {
     heap: BinaryHeap<Candidate>,
     k: usize,
